import { PREFIX, URL_PREFIX } from './common';
import GLOBAL from './global';
import STATIC from './static';
import USER from './user';

import HOME from './home';
import STANDARDSYSTEM from './standardSystem';

const APIS = {
  USER,
  GLOBAL,
  STATIC,
  HOME,
  STANDARDSYSTEM,
};

export { APIS, PREFIX, URL_PREFIX };
